# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Raw bulk data (too large for git)
bulk-data/
data/fullcatalog/dailymed_fullcatalog_raw.csv

# Large derived CSVs (regenerable from scripts + validation tables; one
# exceeds GitHub's 100 MB per-file limit).
data/fullcatalog/allergens_fullcatalog_flagged.csv
data/fullcatalog/gluten_fullcatalog_filtered.csv
data/fullcatalog/gluten_fullcatalog_flagged.csv

# Crash-recovery / cache files generated by the pilot extractor
*.partial
.setid_cache.json

# Editor / IDE
.vscode/
.idea/
*.swp

# Claude
.claude/
CLAUDE.md
